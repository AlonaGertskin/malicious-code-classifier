#include <stdio.h>
#include <string.h>

int main(void){
  // char* str = "ZZZZ";
  char str[] = "ZZZZ";
  printf("%s\n", str);
  float fnum = 781.035217;
  strncpy(str, (char*)&fnum, 4);
  printf("%s\n", str);
  return 0;
}

# --- New Code Block --- 

ZZZZ
ABCD