void ReverseBytes(uint64_t* dst, const uint64_t* src, size_t size)
{
    *dst = 0;
    
    uint8_t* d = (uint8_t*)dst;
    const uint8_t* s = (const uint8_t*)src;

    for (size_t i = size; i--;)
        d[i] = s[(size - 1) - i];
}