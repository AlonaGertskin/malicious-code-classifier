struct foo
{
  uint32_t a;
  uint16_t b;
};

# --- New Code Block --- 

int main(void)
{
  struct foo S;
  uint32_t x = 3;
  uint16_t y = 7;
  memcpy(&S.a, &x, sizeof(x));
  memcpy(&S.b, &y, sizeof(y));
  ...
}