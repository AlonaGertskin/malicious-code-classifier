figures = []

# populate the list
for dataset in datasets:
    figures.append(get_plot(data))

for figure in figures:
    canvases.append(FigureCanvas(figure))

# create a display_widget here

for canvas in canvases:
    my_graph_display.addTab(display_widget, f"{canvas.get_title()}")