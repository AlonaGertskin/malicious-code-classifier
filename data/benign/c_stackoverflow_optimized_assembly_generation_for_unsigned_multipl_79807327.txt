#include <stdint.h>

uint32_t test(uint16_t x)
{
    uint32_t result = (uint32_t)64000 * (uint32_t)x;

    return result;
}

# --- New Code Block --- 

test:
    lsls    r3, r0, #5 // 1 cycle
    subs    r3, r3, r0 // 1 cycle
    lsls    r3, r3, #2 // 1 cycle
    adds    r0, r3, r0 // 1 cycle
    lsls    r0, r0, #9 // 1 cycle
    bx      lr         // 5 cycles total

# --- New Code Block --- 

test:
    movs    r3, #250   // 1 cycle
    lsls    r3, r3, #8 // 1 cycle
    muls    r0, r3     // 1 cycle
    bx      lr         // 3 cycles total